# STACK — `<your-name>` overrides

This kit inherits the recommended sovereign stack from [`frankxai/Starlight-Intelligence-System/STACK.md`](https://github.com/frankxai/Starlight-Intelligence-System/blob/main/STACK.md). That file documents the default choices across L0 (file + version control) through L6 (attestation + audit). Read it once; adopt what resonates.

This file declares only your **overrides** — places where your domain demands a different choice than the recommended default. Every override should name the layer, the substitute, and the reason.

## Your overrides

| Layer | Default | Your choice | Reason |
|-------|---------|-------------|--------|
| `<L?>` | `<default tool / pattern>` | `<your substitute>` | `<why your domain demands it>` |
| ... | ... | ... | ... |

If you have no overrides yet, leave the table empty. The empty state is the strongest signal — you accept the recommended stack as-is.

---

**Built on SIP** · v1.1.0 · MIT
